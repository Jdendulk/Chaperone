<div class="meeting-chat-message" id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.email %></strong>
    <i><%= message.created_at.in_time_zone("London").strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
